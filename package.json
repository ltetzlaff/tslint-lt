{
  "name": "tslint-lt",
  "version": "1.8.0",
  "description": "personal tslint config",
  "scripts": {
    "build": "tsc --watch || true",
    "buildOnce": "tsc",
    "test": "npm run buildOnce && node dist/tests/test.js",
    "prepare": "npm run buildOnce"
  },
  "files": [
    "dist",
    ".prettierrc.yml"
  ],
  "main": "dist/index",
  "types": "dist/index",
  "repository": {
    "type": "git",
    "url": "https://github.com/ltetzlaff/tslint-lt.git"
  },
  "author": "ltetzlaff",
  "license": "All rights reserved.",
  "devDependencies": {
    "@types/node": "^12.0.4",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "prettier": "^1.17.1",
    "tslint": "^5.17.0",
    "tslint-config-prettier": "^1.18.0"
  }
}
